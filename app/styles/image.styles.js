import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    exercise_image_square_52: {
        height: 60,
        width: 60,
        borderRadius: 10,
    },
})

export default styles