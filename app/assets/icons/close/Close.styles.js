import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  close: {
    height: 30,
    width: 30,
    backgroundColor: 'rgba(2, 118, 255, 0.5)',
    borderRadius: 15,
    justifyContent: 'center',
    alignItems: 'center',
  },
})

export default styles
